A basic on-chain price oracle mock with an owner-settable price and a simple read interface.

    lines.append("\nПо типам объектов:")
    for typ, count in by_type.most_common():
        lines.append(f"  - {typ}: {count}")
